{
  "name": "kiosk-display",
  "version": "1.0.0",
  "description": "A custom kiosk display for Inovus Labs, designed to be displayed on a 1080x1920 TV screen in portrait orientation.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bun run scripts/generate.js",
    "preview": "bun run scripts/generate.js && open out/index.html"
  },
  "dependencies": {
    "fast-xml-parser": "^5.6.0",
    "qrcode": "^1.5.4"
  }
}
