{
  "name": "kiosk-ghost-webhook",
  "private": true,
  "scripts": {
    "build": "echo 'no build step required for plain JS Worker'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
